// let filterButton = document.querySelectorAll(".filter-button  button");
// let filterableCard = document.querySelectorAll(".filterable-card .card")
// console.log(filterButton);
// console.log(filterableCard);

// let filterCard = (e) => {
//     // add here active functionality
//     document.querySelector(".active").classList.remove('active');
//     e.target.classList.add('active')

//     filterableCard.forEach(card => {
//         card.classList.add('hidden');

//         if (card.dataset.name === e.target.dataset.name || card.dataset.name === 'all') {
//             card.classList.remove('hidden');

//         }
//     })

// };


// // add click event in all buttons
// filterButton.forEach(button => button.addEventListener('click', filterCard))